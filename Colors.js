let colors = ["red", "blue", "green"];

console.log(colors);

function addColor() {
    let newColor = prompt("Enter a color you like:");
        colors.push(newColor);
            console.log(colors); 
            }

            addColor(); 